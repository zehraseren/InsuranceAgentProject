@model PolicyAnalysisViewModel

@{
    ViewData["Title"] = "AnalyzePdf";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="card">
    <div class="card-body">
        <h5 class="mb-3">PDF Yükle → AI ile Poliçe Analizi</h5>

        @if (!string.IsNullOrEmpty(Model.Error))
        {
            <div class="alert alert-danger">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>@Model.Error
            </div>
        }

        <form asp-action="AnalyzePdf" asp-controller="PolicyAnalysisAI" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label fw-bold">Poliçe PDF Dosyası</label>
                <input type="file" name="pdfFile" class="form-control" accept="application/pdf" required />
            </div>
            <button class="btn btn-primary" type="submit">
                <i class="bi bi-cpu"></i> AI ile Analiz Et
            </button>
        </form>

        @if (!string.IsNullOrEmpty(Model.AnalysisResult))
        {
            <hr class="my-4" />
            <div class="d-flex align-items-center mb-3">
                <i class="bi bi-robot text-primary fs-4 me-2"></i>
                <h5 class="mb-0">AI Analiz Sonucu</h5>
            </div>

            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <div id="markdown-content" class="markdown-body">
                        @Model.AnalysisResult
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<!-- Marked.js ile Markdown'ı HTML'e çevirme -->
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

<style>
    /* Modern Markdown Stilleri */
    .markdown-body {
        font-size: 15px;
        line-height: 1.7;
        color: #24292f;
        overflow-wrap: break-word;
        word-wrap: break-word;
        word-break: break-word;
        overflow-x: hidden;
    }

    .markdown-body h1, .markdown-body h2 {
        border-bottom: 2px solid #e5e7eb;
        padding-bottom: 0.5rem;
        margin-top: 1.5rem;
        margin-bottom: 1rem;
        font-weight: 600;
    }

    .markdown-body h3 {
        margin-top: 1.5rem;
        margin-bottom: 0.75rem;
        font-weight: 600;
        color: #1f2937;
    }

    .markdown-body ul, .markdown-body ol {
        padding-left: 2rem;
        margin-bottom: 1rem;
    }

    .markdown-body li {
        margin-bottom: 0.5rem;
    }

    .markdown-body strong {
        color: #dc2626;
        font-weight: 600;
        background: #fef2f2;
        padding: 2px 6px;
        border-radius: 4px;
    }

    .markdown-body p {
        margin-bottom: 1rem;
    }

    .markdown-body code {
        background: #f3f4f6;
        padding: 2px 6px;
        border-radius: 4px;
        font-size: 0.9em;
        color: #dc2626;
    }

    .markdown-body blockquote {
        border-left: 4px solid #3b82f6;
        padding-left: 1rem;
        margin: 1rem 0;
        color: #6b7280;
        font-style: italic;
    }

    /* Animasyon */
    .card {
        animation: fadeIn 0.5s ease-in;
    }

    .card-body {
        overflow-x: hidden;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>

<script>
    // Sayfa yüklendiğinde markdown'ı işleme
    document.addEventListener('DOMContentLoaded', function() {
        const markdownContent = document.getElementById('markdown-content');
        
        if (markdownContent && typeof marked !== 'undefined') {
            const rawText = markdownContent.textContent;
            
            // Marked.js konfigürasyonu
            marked.setOptions({
                breaks: true,
                gfm: true
            });
            
            // Markdown'ı HTML'e çevirme
            markdownContent.innerHTML = marked.parse(rawText);
        }
    });
</script>